## 1. Environment & Setup

- [x] 1.1 Install python dependencies (`flask`, `pandas`, `scikit-learn`) using pip

## 2. Server Implementation

- [x] 2.1 Implement `app.py` Flask app to initialize SQLite database and serve routes
- [x] 2.2 Configure route `/` to serve `index.html` static dashboard
- [x] 2.3 Implement POST `/api/add_data` and GET `/api/get_data` endpoints in `app.py`

## 3. Verification

- [x] 3.1 Start Flask server and verify homepage loads
- [x] 3.2 Add sensor readings via UI and verify they are saved to database and rendered on chart

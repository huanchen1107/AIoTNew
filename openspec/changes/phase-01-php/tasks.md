## 1. Core Database & Backend API

- [x] 1.1 Create `addData.php` to initialize SQLite3 `aiotdb.db` and insert readings
- [x] 1.2 Create `getData.php` to query and return JSON list of readings

## 2. Frontend Visualizer

- [x] 2.1 Create `index.html` with glassmorphic styling and dual-axis Highcharts.js
- [x] 2.2 Implement mock sensor auto-simulation and terminal logs in `index.html`

## 3. Environment & Verification

- [x] 3.1 Verify database schema, defaults, and triggers using automated python test script
- [x] 3.2 Add `.gitattributes` to enforce correct Unix line endings for shell scripts
